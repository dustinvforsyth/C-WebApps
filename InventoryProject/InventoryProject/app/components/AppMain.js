BASE.require([
    "jQuery"

], function () {

    BASE.namespace("app.components");

    app.components.AppMain = function (elem, tags, scope) {
        var self = this;
        var $elem = $(elem);
    };
});