BASE.require([
    'jQuery'
], function () {
    BASE.namespace('app.components.states');

    app.components.states.MainState = function (elem, tags, scope) {
        var self = this;
        var $elem = $(elem);

    }

})