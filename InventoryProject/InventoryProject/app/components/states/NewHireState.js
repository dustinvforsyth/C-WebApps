BASE.require([
    "jQuery",

], function () {

    BASE.namespace("app.components.states");

    app.components.states.NewHireState = function (elem, tags, scope) {
        var self = this;
        var $elem = $(elem);

    }


});